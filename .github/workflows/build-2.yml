name: Conda build

on:
  repository_dispatch:
    types:
      - conda-build-2

jobs:
  build:
    uses: sot/skare3/.github/workflows/package_build.yml@reusable_workflows
    with:
      meta_packages: ${{ github.event.client_payload.package }}
      package: ""
      tag: ${{ github.event.client_payload.tag }}
      noarch: ${{ !github.event.client_payload.arch }}
    secrets:
      CONDA_PASSWORD: ${{ secrets.CONDA_PASSWORD }}
      CHANDRA_XRAY_TOKEN: ${{ secrets.CHANDRA_XRAY_TOKEN }}
      token: ${{ secrets.GITHUB_TOKEN }}

  upload:
    if: ${{ github.event.client_payload.upload }}
    needs: [build]
    uses: sot/skare3/.github/workflows/package_upload.yml@master
